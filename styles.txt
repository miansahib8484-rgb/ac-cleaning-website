<style>
    *,
    *::before,
    *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0
    }

    html,
    body {
      overflow-x: hidden;
      max-width: 100%;
      width: 100%
    }

    :root {
      --teal: #00c6b2;
      --teal2: #00a396;
      --teal3: #007d70;
      --tealgl: rgba(0, 198, 178, .12);
      --green: #10b981;
      --green2: #059669;
      --green3: #047857;
      --amber: #f5a623;
      --navy: #08141f;
      --navy2: #0d1e2d;
      --navy3: #122436;
      --off: #f3f6f8;
      --gray: #64748b;
      --light: #94a3b8;
      --border: #e2e8f0;
      --r: 6px;
    }

    html {
      scroll-behavior: smooth
    }

    body {
      font-family: "Plus Jakarta Sans", sans-serif;
      color: #1e293b;
      background: #fff;
      -webkit-font-smoothing: antialiased
    }

    img {
      display: block;
      max-width: 100%
    }

    a {
      text-decoration: none;
      color: inherit
    }

    ul {
      list-style: none
    }

    ::selection {
      background: var(--teal);
      color: #fff
    }

    ::-webkit-scrollbar {
      width: 5px
    }

    ::-webkit-scrollbar-track {
      background: #08141f
    }

    ::-webkit-scrollbar-thumb {
      background: var(--teal);
      border-radius: 3px
    }

    .ar {
      font-family: "Tajawal", sans-serif;
      direction: rtl;
      display: block;
      font-size: 1.15em;
      color: var(--teal);
      font-weight: 700;
      margin-top: 8px;
      letter-spacing: 0;
      line-height: 1.5
    }

    .ar-white {
      color: rgba(255, 255, 255, .65)
    }

    /* ══ TOPBAR ══ */
    .topbar {
      background: var(--navy);
      padding: 9px 40px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 22px;
      flex-wrap: wrap;
      overflow: visible
    }

    .topbar-row2 {
      display: contents
    }

    .ti {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 12.5px;
      color: rgba(255, 255, 255, .6);
      font-weight: 500;
      white-space: nowrap;
      flex-shrink: 0
    }

    .ti svg {
      width: 13px;
      height: 13px;
      fill: var(--teal);
      flex-shrink: 0
    }

    .ti a {
      color: rgba(255, 255, 255, .8);
      transition: color .2s
    }

    .ti a:hover {
      color: var(--teal)
    }

    .tdiv {
      width: 1px;
      height: 14px;
      background: rgba(255, 255, 255, .15);
      flex-shrink: 0
    }

    /* ══ HEADER / NAV ══ */
    header {
      position: sticky;
      top: 0;
      z-index: 999;
      background: rgba(255, 255, 255, .97);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
      transition: box-shadow .3s;
      width: 100%
    }

    header.scrolled {
      box-shadow: 0 2px 30px rgba(0, 0, 0, .1)
    }

    nav {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 40px;
      height: 84px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      width: 100%
    }

    .logo-wrap {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-shrink: 0;
      text-decoration: none
    }

    .logo-wrap img {
      height: 60px;
      width: auto;
      object-fit: contain
    }

    .logo-text-block {
      display: flex;
      flex-direction: column;
      line-height: 1.25
    }

    .logo-company {
      font-family: "Fraunces", serif;
      font-size: 21px;
      font-weight: 900;
      color: var(--navy);
      white-space: nowrap;
      letter-spacing: -.3px
    }

    .logo-company span {
      color: var(--teal)
    }

    .logo-sub {
      font-family: "Tajawal", sans-serif;
      font-size: 16px;
      color: var(--gray);
      direction: rtl;
      margin-top: 2px;
      font-weight: 600
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 2px
    }

    .nav-links a {
      font-size: 13.5px;
      font-weight: 600;
      color: #475569;
      padding: 7px 13px;
      border-radius: var(--r);
      transition: all .2s;
      white-space: nowrap
    }

    .nav-links a:hover,
    .nav-links a.active {
      color: var(--teal);
      background: var(--tealgl)
    }

    .nav-right {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-shrink: 0
    }

    /* ══ HAMBURGER ══ */
    .hamburger {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
      padding: 8px;
      border: none;
      background: transparent;
      border-radius: var(--r);
      transition: background .2s;
      flex-shrink: 0
    }

    .hamburger:hover {
      background: var(--tealgl)
    }

    .hamburger span {
      display: block;
      width: 22px;
      height: 2.5px;
      background: var(--navy);
      border-radius: 2px;
      transition: all .3s
    }

    .hamburger.open span:nth-child(1) {
      transform: translateY(7.5px) rotate(45deg)
    }

    .hamburger.open span:nth-child(2) {
      opacity: 0;
      transform: scaleX(0)
    }

    .hamburger.open span:nth-child(3) {
      transform: translateY(-7.5px) rotate(-45deg)
    }

    /* ══ MOBILE DRAWER ══ */
    .mob-menu {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(8, 20, 31, .97);
      z-index: 9999;
      flex-direction: column;
      padding: 72px 28px 36px;
      overflow-y: auto;
      backdrop-filter: blur(16px)
    }

    .mob-menu.open {
      display: flex
    }

    .mob-menu a.mob-link {
      font-size: 20px;
      font-weight: 800;
      color: #fff;
      padding: 18px 0;
      border-bottom: 1px solid rgba(255, 255, 255, .08);
      transition: color .2s;
      display: flex;
      align-items: center;
      justify-content: space-between
    }

    .mob-menu a.mob-link:hover {
      color: var(--teal)
    }

    .mob-menu a.mob-link::after {
      content: "→";
      opacity: .3;
      font-size: 16px
    }

    .mob-close {
      position: absolute;
      top: 18px;
      right: 18px;
      background: rgba(255, 255, 255, .08);
      border: none;
      color: #fff;
      width: 42px;
      height: 42px;
      border-radius: 10px;
      font-size: 20px;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background .2s
    }

    .mob-close:hover {
      background: rgba(255, 255, 255, .18)
    }

    .mob-wa-btn {
      background: #25D366;
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 16px 24px;
      font-size: 16px;
      font-weight: 800;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      margin-top: 24px;
      box-shadow: 0 4px 16px rgba(37, 211, 102, .4);
      transition: all .3s;
      text-decoration: none
    }

    .mob-wa-btn:hover {
      background: #128C7E;
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(18, 140, 126, .5)
    }

    .mob-wa-btn svg {
      width: 20px;
      height: 20px;
      fill: #fff;
      flex-shrink: 0
    }

    /* ══ BUTTONS ══ */
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 12px 24px;
      font-size: 14px;
      font-weight: 700;
      border-radius: 8px;
      cursor: pointer;
      transition: all .25s ease;
      border: 2px solid transparent;
      font-family: "Plus Jakarta Sans", sans-serif;
      white-space: nowrap;
      box-shadow: 0 2px 8px rgba(0, 0, 0, .1);
      text-decoration: none
    }

    .btn svg {
      width: 16px;
      height: 16px;
      fill: currentColor;
      flex-shrink: 0
    }

    .btn-green {
      background: #25D366;
      color: #fff;
      border: 2px solid #25D366;
      border-radius: 8px;
      box-shadow: 0 4px 14px rgba(16, 185, 129, .3)
    }

    .btn-green:hover {
      background: transparent;
      color: #25D366;
      border: 2px solid #25D366;
      ;
      box-shadow: 0 6px 20px rgba(16, 185, 129, .2)
    }

    .btn-green svg {
      width: 18px;
      height: 18px;
      fill: currentColor
    }

    .btn-outline {
      background: transparent;
      border: 2px solid #25D366;
      color: #25D366;
      box-shadow: 0 2px 8px rgba(16, 185, 129, .15)
    }

    .btn-outline:hover {
      background: #25D366;
      color: #fff;
      box-shadow: 0 4px 16px rgba(16, 185, 129, .35)
    }

    .btn-ghost {
      background: rgba(255, 255, 255, .1);
      border: 2px solid rgba(255, 255, 255, .28);
      color: #fff;
      backdrop-filter: blur(8px)
    }

    .btn-ghost:hover {
      background: rgba(255, 255, 255, .2);
      border-color: rgba(255, 255, 255, .4)
    }

    .btn-ghost svg {
      fill: #fff
    }

    .btn-wa {
      background: #25D366;
      color: #fff;
      border: 2px solid #25D366;
      border-radius: 8px;
      box-shadow: 0 4px 16px rgba(37, 211, 102, .35);
      padding: 12px 24px;
      font-size: 14px
    }

    .btn-wa:hover {
      background: transparent;
      color: #25D366;
      border: 2px solid #25D366;
      transform: translateY(-3px);
      box-shadow: 0 8px 24px rgba(18, 140, 126, .2)
    }

    .btn-wa svg {
      width: 18px;
      height: 18px;
      fill: currentColor
    }

    .btn-wa-nav {
      background: #25D366;
      color: #fff;
      border: 2px solid #25D366;
      border-radius: 8px;
      box-shadow: 0 3px 10px rgba(37, 211, 102, .3);
      padding: 12px 20px;
      font-size: 13.5px
    }

    .btn-wa-nav:hover {
      background: transparent;
      color: #25D366;
      border: 2px solid #25D366;
      transform: translateY(-2px);
      box-shadow: 0 5px 16px rgba(18, 140, 126, .2)
    }

    .btn-wa-nav svg {
      width: 16px;
      height: 16px;
      fill: currentColor
    }

    .btn-lg {
      padding: 14px 30px;
      font-size: 15px;
      border-radius: 10px
    }

    .btn-lg svg {
      width: 18px;
      height: 18px
    }

    .btn-lg.btn-wa,
    .btn-lg.btn-green {
      border-radius: 8px
    }

    .btn-lg.btn-wa svg,
    .btn-lg.btn-green svg {
      width: 20px;
      height: 20px
    }

    .btn-xl {
      padding: 16px 34px;
      font-size: 16px;
      border-radius: 10px
    }

    .btn-xl svg {
      width: 18px;
      height: 18px
    }

    .btn-xl.btn-wa,
    .btn-xl.btn-green {
      border-radius: 8px
    }

    .btn-xl.btn-wa svg,
    .btn-xl.btn-green svg {
      width: 20px;
      height: 20px
    }

    /* ══ HERO ══ */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      overflow: hidden;
      background: var(--navy)
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      background: url("https://images.unsplash.com/photo-1558618666-fcd25c85cd64?auto=format&fit=crop&w=1920&q=90") center/cover no-repeat;
      opacity: .22
    }

    .hero-grad {
      position: absolute;
      inset: 0;
      background: linear-gradient(110deg, rgba(8, 20, 31, .97) 50%, rgba(8, 20, 31, .65) 75%, rgba(8, 20, 31, .3) 100%)
    }

    .hero-orb1 {
      position: absolute;
      width: 600px;
      height: 600px;
      border-radius: 50%;
      background: rgba(0, 198, 178, .08);
      filter: blur(90px);
      top: -100px;
      right: 0;
      pointer-events: none;
      animation: orbF 9s ease-in-out infinite
    }

    .hero-orb2 {
      position: absolute;
      width: 400px;
      height: 400px;
      border-radius: 50%;
      background: rgba(245, 166, 35, .06);
      filter: blur(70px);
      bottom: -60px;
      right: 35%;
      pointer-events: none;
      animation: orbF 13s ease-in-out infinite reverse
    }

    @keyframes orbF {

      0%,
      100% {
        transform: translateY(0)
      }

      50% {
        transform: translateY(-28px)
      }
    }

    .hero-inner {
      position: relative;
      z-index: 2;
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 40px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 60px;
      align-items: center;
      width: 100%;
      min-height: 100vh
    }

    .hero-left {
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 80px 0
    }

    .hero-pill {
      display: inline-flex;
      align-items: center;
      gap: 9px;
      background: rgba(0, 198, 178, .1);
      border: 1px solid rgba(0, 198, 178, .25);
      padding: 8px 18px;
      border-radius: 100px;
      margin-bottom: 26px;
      font-size: 11.5px;
      font-weight: 700;
      color: var(--teal);
      text-transform: uppercase;
      letter-spacing: 1px;
      width: fit-content
    }

    .hero-pill-dot {
      width: 7px;
      height: 7px;
      background: var(--teal);
      border-radius: 50%;
      animation: ping 2s infinite
    }

    @keyframes ping {

      0%,
      100% {
        box-shadow: 0 0 0 0 rgba(0, 198, 178, .5)
      }

      50% {
        box-shadow: 0 0 0 5px rgba(0, 198, 178, .08)
      }
    }

    .hero h1 {
      font-family: "Fraunces", serif;
      font-size: clamp(25px, 4.2vw, 60px);
      color: #fff;
      line-height: 1.08;
      margin-bottom: 6px;
      font-weight: 700
    }

    .hero h1 .accent {
      color: var(--teal);
      font-style: italic
    }

    .hero-ar-title {
      font-family: "Tajawal", sans-serif;
      direction: rtl;
      font-size: clamp(20px, 3.2vw, 36px);
      color: rgba(255, 255, 255, .55);
      font-weight: 700;
      margin-bottom: 22px;
      line-height: 1.4
    }

    .hero-desc {
      font-size: 15.5px;
      color: rgba(255, 255, 255, .58);
      line-height: 1.82;
      margin-bottom: 34px;
      max-width: 520px;
      font-weight: 300
    }

    .hero-btns {
      display: flex;
      gap: 14px;
      align-items: center;
      flex-wrap: wrap;
      margin-bottom: 40px
    }

    .hero-trust {
      display: flex;
      align-items: center;
      gap: 16px;
      padding-top: 34px;
      border-top: 1px solid rgba(255, 255, 255, .09)
    }

    .hero-avatars {
      display: flex
    }

    .hero-avatars img {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      border: 2.5px solid rgba(255, 255, 255, .2);
      object-fit: cover;
      margin-left: -8px
    }

    .hero-avatars img:first-child {
      margin-left: 0
    }

    .trust-stars {
      color: #f5a623;
      font-size: 12.5px;
      letter-spacing: 1px
    }

    .trust-lbl {
      font-size: 11.5px;
      color: rgba(255, 255, 255, .42);
      margin-top: 2px
    }

    .hero-badges {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-top: 30px
    }

    .hbadge {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 11.5px;
      color: rgba(255, 255, 255, .48);
      font-weight: 500
    }

    .hbadge svg {
      width: 13px;
      height: 13px;
      fill: var(--teal)
    }

    .hero-right {
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      padding: 60px 0
    }

    .hero-img-wrap {
      position: relative;
      width: 100%;
      max-width: 500px
    }

    .hero-main-img {
      width: 100%;
      height: auto;
      min-height: 480px;
      object-fit: cover;
      border-radius: 24px;
      box-shadow: 0 40px 100px rgba(0, 0, 0, .5)
    }

    .hero-fc {
      position: absolute;
      background: rgba(255, 255, 255, .95);
      backdrop-filter: blur(12px);
      border-radius: 14px;
      padding: 14px 18px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, .18);
      display: flex;
      align-items: center;
      gap: 12px
    }

    .hero-fc1 {
      bottom: 32px;
      left: -24px
    }

    .hero-fc2 {
      top: 40px;
      right: -20px
    }

    .hero-fc .hf-ico {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: var(--tealgl);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0
    }

    .hero-fc .hf-ico svg {
      width: 20px;
      height: 20px;
      fill: var(--teal)
    }

    .hf-val {
      font-size: 18px;
      font-weight: 900;
      color: var(--navy);
      line-height: 1
    }

    .hf-lbl {
      font-size: 11px;
      color: var(--gray);
      margin-top: 2px
    }

    .hero-img-badge {
      position: absolute;
      top: 32px;
      left: -20px;
      background: linear-gradient(135deg, var(--teal), var(--teal3));
      border-radius: 12px;
      padding: 12px 16px;
      text-align: center;
      box-shadow: 0 8px 28px rgba(0, 198, 178, .4)
    }

    .hib-num {
      font-family: "Fraunces", serif;
      font-size: 28px;
      font-weight: 900;
      color: #fff;
      line-height: 1
    }

    .hib-txt {
      font-size: 10.5px;
      font-weight: 700;
      color: rgba(255, 255, 255, .8);
      text-transform: uppercase;
      letter-spacing: .5px;
      margin-top: 2px
    }

    /* ══ TICKER ══ */
    .ticker {
      background: var(--teal);
      padding: 11px 0;
      overflow: hidden;
      white-space: nowrap
    }

    .ticker-track {
      display: inline-flex;
      animation: scroll 32s linear infinite
    }

    .ticker-track:hover {
      animation-play-state: paused
    }

    .tick-item {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      font-size: 13px;
      font-weight: 700;
      color: #fff;
      text-transform: uppercase;
      letter-spacing: .4px;
      padding: 0 28px
    }

    .tick-sep {
      color: rgba(255, 255, 255, .4);
      font-size: 15px
    }

    @keyframes scroll {
      0% {
        transform: translateX(0)
      }

      100% {
        transform: translateX(-50%)
      }
    }

    /* ══ SECTIONS ══ */
    .section {
      padding: 90px 40px
    }

    .wrap {
      max-width: 1280px;
      margin: 0 auto
    }

    .sec-eyebrow {
      font-size: 11.5px;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 2.5px;
      color: var(--teal);
      margin-bottom: 10px
    }

    .sec-title {
      font-family: "Fraunces", serif;
      font-size: clamp(24px, 3.8vw, 46px);
      color: var(--navy);
      line-height: 1.15;
      font-weight: 700
    }

    .sec-title em {
      color: var(--teal);
      font-style: italic
    }

    .sec-sub {
      font-size: 15.5px;
      color: var(--gray);
      line-height: 1.8;
      max-width: 510px;
      margin-top: 12px;
      font-weight: 300
    }

    .bg-off {
      background: var(--off)
    }

    /* ══ SERVICES – 3 per row ══ */
    .srv-head {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 50px;
      flex-wrap: wrap;
      gap: 20px
    }

    .srv-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 22px
    }

    .srv-card {
      background: #fff;
      border-radius: 18px;
      padding: 30px 24px 26px;
      border: 1px solid var(--border);
      transition: all .35s;
      position: relative;
      overflow: hidden
    }

    .srv-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--teal), var(--teal3));
      transform: scaleX(0);
      transform-origin: left;
      transition: transform .35s
    }

    .srv-card:hover {
      transform: translateY(-7px);
      box-shadow: 0 26px 66px rgba(0, 0, 0, .1);
      border-color: rgba(0, 198, 178, .18)
    }

    .srv-card:hover::before {
      transform: scaleX(1)
    }

    .srv-num {
      position: absolute;
      top: 16px;
      right: 16px;
      width: 34px;
      height: 34px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--teal), var(--teal3));
      color: #fff;
      font-family: "Fraunces", serif;
      font-size: 13px;
      font-weight: 900;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 14px rgba(0, 198, 178, .38);
      transition: all .35s
    }

    .srv-card:hover .srv-num {
      transform: scale(1.12)
    }

    .srv-ico {
      width: 56px;
      height: 56px;
      background: linear-gradient(135deg, rgba(0, 198, 178, .13), rgba(0, 198, 178, .06));
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 16px;
      transition: all .35s;
      color: var(--teal)
    }

    .srv-card:hover .srv-ico {
      background: linear-gradient(135deg, var(--teal), var(--teal3));
      box-shadow: 0 8px 26px rgba(0, 198, 178, .32);
      color: #fff
    }

    .srv-ico svg {
      width: 28px;
      height: 28px;
      transition: all .35s
    }

    .srv-card h3 {
      font-size: 15.5px;
      font-weight: 800;
      color: var(--navy);
      margin-bottom: 4px;
      line-height: 1.3
    }

    .srv-card p {
      font-size: 13px;
      color: var(--gray);
      line-height: 1.75;
      margin-bottom: 14px
    }

    .srv-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
      margin-bottom: 16px
    }

    .srv-tag {
      font-size: 10.5px;
      font-weight: 600;
      background: rgba(0, 198, 178, .08);
      color: var(--teal);
      padding: 3px 9px;
      border-radius: 100px
    }

    .srv-btn {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      font-size: 13px;
      font-weight: 700;
      color: #fff;
      background: linear-gradient(135deg, #10b981, #059669);
      border: 2px solid #059669;
      padding: 10px 18px;
      border-radius: 8px;
      transition: all .25s;
      box-shadow: 0 3px 10px rgba(16, 185, 129, .25)
    }

    .srv-btn:hover {
      background: linear-gradient(135deg, #059669, #047857);
      box-shadow: 0 5px 16px rgba(16, 185, 129, .4);
      transform: translateY(-2px)
    }

    .srv-btn svg {
      width: 13px;
      height: 13px;
      fill: #fff;
      transition: all .22s
    }

    .srv-btn:hover svg {
      transform: translateX(3px)
    }

    /* ══ STATS ══ */
    .stats-strip {
      background: var(--navy2)
    }

    .stats-row {
      max-width: 1280px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(4, 1fr)
    }

    .stat-col {
      padding: 50px 30px;
      text-align: center;
      border-right: 1px solid rgba(255, 255, 255, .07);
      transition: background .3s
    }

    .stat-col:last-child {
      border-right: none
    }

    .stat-col:hover {
      background: rgba(0, 198, 178, .07)
    }

    .stat-num {
      font-family: "Fraunces", serif;
      font-size: 52px;
      font-weight: 900;
      line-height: 1;
      background: linear-gradient(135deg, var(--teal), #7ffff4);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text
    }

    .stat-num sup {
      font-size: 20px
    }

    .stat-lbl {
      font-size: 13px;
      color: rgba(255, 255, 255, .42);
      margin-top: 9px;
      line-height: 1.55
    }

    /* ══ PROCESS ══ */
    .process-bg {
      background: linear-gradient(160deg, var(--navy), var(--navy2));
      padding: 90px 40px
    }

    .process-head {
      text-align: center;
      margin-bottom: 56px
    }

    .process-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 18px;
      max-width: 1280px;
      margin: 0 auto
    }

    .step {
      background: rgba(255, 255, 255, .04);
      border: 1px solid rgba(255, 255, 255, .07);
      border-radius: 18px;
      padding: 38px 24px;
      text-align: center;
      transition: all .3s
    }

    .step:hover {
      background: rgba(0, 198, 178, .07);
      border-color: rgba(0, 198, 178, .22);
      transform: translateY(-6px)
    }

    .step-num {
      width: 56px;
      height: 56px;
      background: linear-gradient(135deg, var(--teal), var(--teal3));
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Fraunces", serif;
      font-size: 22px;
      font-weight: 900;
      color: #fff;
      margin: 0 auto 18px;
      box-shadow: 0 8px 24px rgba(0, 198, 178, .3)
    }

    .step h4 {
      font-size: 15.5px;
      font-weight: 800;
      color: #fff;
      margin-bottom: 4px
    }

    .step p {
      font-size: 13px;
      color: rgba(255, 255, 255, .48);
      line-height: 1.72
    }

    /* ══ WHY CHOOSE US ══ */
    .why-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 72px;
      align-items: center
    }

    .why-visual {
      position: relative
    }

    .why-video-container {
      position: relative;
      width: 100%;
      max-width: 380px;
      margin: 0 auto;
      aspect-ratio: 9/16;
      border-radius: 20px;
      overflow: hidden;
      background: #000;
      box-shadow: 0 40px 80px rgba(0, 0, 0, .25)
    }

    .why-video-container video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block
    }

    .why-video-poster {
      position: absolute;
      inset: 0;
      background: var(--navy2);
      transition: opacity .45s
    }

    .why-video-poster.hidden {
      opacity: 0;
      pointer-events: none
    }

    .why-stop-btn {
      position: absolute;
      top: 12px;
      right: 12px;
      width: 36px;
      height: 36px;
      background: rgba(0, 0, 0, .6);
      border: none;
      border-radius: 50%;
      display: none;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      z-index: 10;
      transition: background .2s
    }

    .why-stop-btn.visible {
      display: flex
    }

    .why-stop-btn:hover {
      background: rgba(255, 0, 0, .7)
    }

    .why-stop-btn svg {
      width: 16px;
      height: 16px;
      fill: #fff
    }

    .why-play-overlay {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: background.3s
    }

    .why-play-overlay:hover {
      background: rgba(0, 0, 0, .12)
    }

    .why-play-overlay.hidden {
      display: none
    }

    .why-play-btn-circle {
      width: 76px;
      height: 76px;
      background: linear-gradient(135deg, #f5a623, #e07b0f);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 0 0 18px rgba(245, 166, 35, .18), 0 12px 36px rgba(245, 166, 35, .4);
      transition: transform .3s
    }

    .why-play-overlay:hover .why-play-btn-circle {
      transform: scale(1.1)
    }

    .why-play-btn-circle svg {
      width: 28px;
      height: 28px;
      fill: #fff;
      margin-left: 5px
    }

    .why-float {
      position: absolute;
      bottom: 28px;
      left: 50%;
      transform: translateX(-50%);
      background: #fff;
      border-radius: 14px;
      padding: 16px 20px;
      box-shadow: 0 12px 40px rgba(0, 0, 0, .13);
      display: flex;
      align-items: center;
      gap: 13px;
      z-index: 5
    }

    .wf-ico {
      width: 40px;
      height: 40px;
      background: var(--tealgl);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0
    }

    .wf-ico svg {
      width: 19px;
      height: 19px;
      fill: var(--teal)
    }

    .wf-num {
      font-family: "Fraunces", serif;
      font-size: 21px;
      font-weight: 900;
      color: var(--navy);
      line-height: 1
    }

    .wf-lbl {
      font-size: 11.5px;
      color: var(--gray);
      margin-top: 2px
    }

    .why-pts {
      display: flex;
      flex-direction: column;
      gap: 16px;
      margin-top: 30px
    }

    .why-pt {
      display: flex;
      gap: 14px;
      padding: 18px;
      border-radius: 14px;
      border: 1.5px solid var(--border);
      background: #fff;
      transition: all .3s
    }

    .why-pt:hover {
      border-color: rgba(0, 198, 178, .25);
      box-shadow: 0 8px 28px rgba(0, 0, 0, .06);
      transform: translateX(4px)
    }

    .wp-ico {
      width: 40px;
      height: 40px;
      border-radius: 10px;
      background: var(--tealgl);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      transition: background .3s
    }

    .why-pt:hover .wp-ico {
      background: linear-gradient(135deg, var(--teal), var(--teal3))
    }

    .wp-ico svg {
      width: 18px;
      height: 18px;
      fill: var(--teal);
      transition: fill .3s
    }

    .why-pt:hover .wp-ico svg {
      fill: #fff
    }

    .wp-title {
      font-size: 14.5px;
      font-weight: 800;
      color: var(--navy);
      margin-bottom: 3px
    }

    .wp-desc {
      font-size: 13px;
      color: var(--gray);
      line-height: 1.65
    }

    /* ══ GALLERY ══ */
    .gal-head {
      text-align: center;
      margin-bottom: 48px
    }

    .gal-mosaic {
      display: grid;
      grid-template-columns: repeat(12, 1fr);
      grid-template-rows: 240px 240px;
      gap: 6px
    }

    .gal-item {
      overflow: hidden;
      border-radius: 12px;
      cursor: pointer;
      position: relative
    }

    .gal-item:nth-child(1) {
      grid-column: span 5;
      grid-row: span 2
    }

    .gal-item:nth-child(2) {
      grid-column: span 4
    }

    .gal-item:nth-child(3) {
      grid-column: span 3
    }

    .gal-item:nth-child(4) {
      grid-column: span 3
    }

    .gal-item:nth-child(5) {
      grid-column: span 4
    }

    .gal-item img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform .55s
    }

    .gal-item:hover img {
      transform: scale(1.07)
    }

    .gal-over {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top, rgba(8, 20, 31, .7), transparent);
      opacity: 0;
      transition: opacity .3s;
      display: flex;
      align-items: flex-end;
      padding: 18px
    }

    .gal-item:hover .gal-over {
      opacity: 1
    }

    .gal-lbl {
      font-size: 12.5px;
      font-weight: 700;
      color: #fff
    }

    /* ══ TESTIMONIALS ══ */
    .test-bg {
      background: linear-gradient(160deg, var(--navy), var(--navy2));
      overflow: hidden
    }

    .test-head {
      text-align: center;
      margin-bottom: 54px
    }

    .test-scroll-container {
      overflow: hidden;
      position: relative;
      padding: 20px 0
    }

    .test-grid {
      display: flex;
      gap: 20px;
      animation: scrollReviews 40s linear infinite;
      width: max-content
    }

    .test-grid:hover {
      animation-play-state: paused
    }

    @keyframes scrollReviews {
      0% {
        transform: translateX(0)
      }

      100% {
        transform: translateX(-50%)
      }
    }

    .test-card {
      background: rgba(255, 255, 255, .04);
      border: 1px solid rgba(255, 255, 255, .08);
      border-radius: 18px;
      padding: 24px 22px;
      transition: all .3s;
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      min-height: 280px;
      width: 380px;
      flex-shrink: 0
    }

    .test-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, var(--teal), transparent);
      opacity: 0;
      transition: opacity .3s
    }

    .test-card:hover {
      background: rgba(255, 255, 255, .07);
      border-color: rgba(0, 198, 178, .2);
      transform: translateY(-5px)
    }

    .test-card:hover::before {
      opacity: 1
    }

    .test-stars {
      color: #f5a623;
      font-size: 14px;
      letter-spacing: 1px;
      margin-bottom: 12px
    }

    .test-q {
      font-family: "Fraunces", serif;
      font-size: 42px;
      color: var(--teal);
      opacity: .18;
      line-height: .7;
      margin-bottom: 12px;
      font-weight: 900
    }

    .test-card p {
      font-size: 13px;
      color: rgba(255, 255, 255, .68);
      line-height: 1.7;
      margin-bottom: auto;
      white-space: normal;
      word-break: break-word;
    }

    .ar-review-text {
      font-family: "Tajawal", sans-serif !important;
      direction: rtl;
      text-align: right;
      font-size: 14.5px !important;
      line-height: 1.8 !important;
      font-weight: 500
    }

    .test-author {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-top: 20px;
      padding-top: 16px;
      border-top: 1px solid rgba(255, 255, 255, .06)
    }

    .test-author-ar {
      flex-direction: row-reverse
    }

    .test-av {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      object-fit: cover;
      border: 2.5px solid rgba(0, 198, 178, .3);
      flex-shrink: 0
    }

    .test-av-ar {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background: linear-gradient(135deg, var(--teal), var(--teal3));
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Tajawal", sans-serif;
      font-size: 15px;
      font-weight: 800;
      color: #fff;
      border: 2.5px solid rgba(0, 198, 178, .4);
      flex-shrink: 0
    }

    .test-name {
      font-size: 13.5px;
      font-weight: 800;
      color: #fff;
      line-height: 1.3
    }

    .test-name-ar {
      font-family: "Tajawal", sans-serif;
      direction: rtl;
      font-size: 16px;
      font-weight: 700
    }

    .test-loc {
      font-size: 10.5px;
      color: rgba(255, 255, 255, .38);
      margin-top: 3px;
      line-height: 1.3
    }

    .test-loc-ar {
      font-family: "Tajawal", sans-serif;
      direction: rtl;
      font-size: 12.5px;
      text-align: right
    }

    .test-badge {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 10px;
      color: var(--teal);
      margin-top: 4px;
      font-weight: 600
    }

    .test-badge svg {
      width: 10px;
      height: 10px;
      fill: var(--teal)
    }

    .test-badge-ar {
      font-family: "Tajawal", sans-serif;
      direction: rtl;
      justify-content: flex-end
    }

    /* ══ FORM ══ */
    .estimate-bg {
      background: var(--off)
    }

    .estimate-head {
      text-align: center;
      margin-bottom: 44px
    }

    .form-card {
      background: #fff;
      border-radius: 22px;
      padding: 52px 48px;
      box-shadow: 0 24px 80px rgba(0, 0, 0, .09);
      border: 1px solid var(--border);
      max-width: 840px;
      margin: 0 auto
    }

    .form-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px
    }

    .ff {
      display: flex;
      flex-direction: column;
      gap: 6px
    }

    .ff.full {
      grid-column: span 2
    }

    .ff label {
      font-size: 12.5px;
      font-weight: 700;
      color: var(--navy)
    }

    .fc {
      background: var(--off);
      border: 1.5px solid var(--border);
      border-radius: 8px;
      padding: 12px 15px;
      font-size: 14px;
      font-family: "Plus Jakarta Sans", sans-serif;
      color: #1e293b;
      outline: none;
      transition: border-color .2s, box-shadow .2s;
      width: 100%
    }

    .fc:focus {
      border-color: var(--teal);
      box-shadow: 0 0 0 3px rgba(0, 198, 178, .1)
    }

    .fc::placeholder {
      color: var(--light)
    }

    textarea.fc {
      resize: vertical;
      min-height: 100px
    }

    .fsr {
      grid-column: span 2;
      display: flex;
      flex-direction: column;
      gap: 12px;
      margin-top: 4px
    }

    .fbadges {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 22px;
      flex-wrap: wrap
    }

    .fbadge-item {
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 12px;
      color: var(--gray)
    }

    .fbadge-item svg {
      width: 13px;
      height: 13px;
      fill: var(--teal)
    }

    /* ══ CTA BAND ══ */
    .cta-outer {
      padding: 60px 0;
      background: #fff
    }

    .cta-band {
      position: relative;
      overflow: hidden;
      border-radius: 0;
      background: linear-gradient(135deg, var(--navy2), var(--navy3))
    }

    .cta-band::before {
      content: "";
      position: absolute;
      inset: 0;
      background: url("https://images.unsplash.com/photo-1621905252507-b35492cc74b4?auto=format&fit=crop&w=1400&q=80") center/cover;
      opacity: .1
    }

    .cta-glow {
      position: absolute;
      width: 500px;
      height: 500px;
      border-radius: 50%;
      background: rgba(0, 198, 178, .08);
      filter: blur(80px);
      top: -100px;
      right: -60px
    }

    .cta-glow2 {
      position: absolute;
      width: 300px;
      height: 300px;
      border-radius: 50%;
      background: rgba(245, 166, 35, .06);
      filter: blur(60px);
      bottom: -50px;
      left: 20%
    }

    .cta-inner {
      position: relative;
      z-index: 1;
      padding: 72px 68px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 44px;
      flex-wrap: wrap;
      max-width: 1280px;
      margin: 0 auto
    }

    .cta-left h2 {
      font-family: "Fraunces", serif;
      font-size: clamp(26px, 3.8vw, 44px);
      color: #fff;
      line-height: 1.18;
      max-width: 520px;
      font-weight: 700
    }

    .cta-left h2 em {
      color: var(--teal);
      font-style: italic
    }

    .cta-left p {
      font-size: 15.5px;
      color: rgba(255, 255, 255, .52);
      margin-top: 12px;
      max-width: 440px;
      line-height: 1.78;
      font-weight: 300
    }

    .cta-right-btns {
      display: flex;
      flex-direction: column;
      gap: 12px;
      align-items: flex-end
    }

    .cta-right-btns .btn {
      min-width: 240px;
      justify-content: center
    }

    .cta-call {
      display: flex;
      align-items: center;
      gap: 8px;
      color: rgba(255, 255, 255, .5);
      font-size: 13px;
      font-weight: 600;
      justify-content: center
    }

    .cta-call svg {
      width: 13px;
      height: 13px;
      fill: var(--teal)
    }

    /* ══ CONTACT BAR ══ */
    .cbar {
      background: linear-gradient(135deg, var(--teal), var(--teal3));
      padding: 0
    }

    .cbar-inner {
      max-width: 100%;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(3, 1fr)
    }

    .ci {
      display: flex;
      align-items: center;
      gap: 16px;
      padding: 30px 24px;
      border-right: 1px solid rgba(255, 255, 255, .18)
    }

    .ci:last-child {
      border-right: none
    }

    .ci-ico {
      width: 46px;
      height: 46px;
      background: rgba(255, 255, 255, .2);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0
    }

    .ci-ico svg {
      width: 21px;
      height: 21px;
      fill: #fff
    }

    .ci-lbl {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: rgba(255, 255, 255, .7);
      font-weight: 700;
      margin-bottom: 3px
    }

    .ci-val {
      font-size: 14px;
      font-weight: 800;
      color: #fff;
      line-height: 1.4
    }

    /* ══ FOOTER ══ */
    .footer {
      background: var(--navy);
      padding: 76px 0 0
    }

    .footer-wrap {
      max-width: 100%;
      margin: 0 auto
    }

    .footer-grid {
      display: grid;
      grid-template-columns: 2.2fr 1fr 1fr 1fr;
      gap: 50px;
      padding: 0 40px 60px;
      max-width: 1280px;
      margin: 0 auto
    }

    .footer-brand {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 16px
    }

    .footer-brand img {
      height: 56px;
      width: auto;
      object-fit: contain;
      filter: brightness(0) invert(1)
    }

    .footer-brand-text .fn {
      font-family: "Fraunces", serif;
      font-size: 16px;
      font-weight: 900;
      color: #fff;
      white-space: nowrap
    }

    .footer-brand-text .fn span {
      color: var(--teal)
    }

    .footer-brand-text .far-sub {
      font-family: "Tajawal", sans-serif;
      font-size: 15px;
      color: rgba(255, 255, 255, .45);
      direction: rtl;
      margin-top: 2px;
      font-weight: 500
    }

    .footer-about p {
      font-size: 13px;
      color: rgba(255, 255, 255, .4);
      line-height: 1.85;
      max-width: 280px
    }

    .footer-contact-info {
      margin-top: 20px;
      display: flex;
      flex-direction: column;
      gap: 10px
    }

    .fci {
      display: flex;
      align-items: flex-start;
      gap: 8px;
      font-size: 13px;
      color: rgba(255, 255, 255, .5)
    }

    .fci svg {
      width: 14px;
      height: 14px;
      fill: var(--teal);
      flex-shrink: 0;
      margin-top: 2px
    }

    .fci a {
      color: rgba(255, 255, 255, .7);
      transition: color .2s
    }

    .fci a:hover {
      color: var(--teal)
    }

    .nl-wrap {
      margin-top: 20px
    }

    .nl-wrap p {
      font-size: 12px;
      color: rgba(255, 255, 255, .45);
      margin-bottom: 8px;
      font-weight: 500
    }

    .nl-row {
      display: flex;
      gap: 8px
    }

    .nl-input {
      flex: 1;
      min-width: 0;
      background: rgba(255, 255, 255, .06);
      border: 1.5px solid rgba(255, 255, 255, .1);
      border-radius: var(--r);
      padding: 10px 13px;
      color: #fff;
      font-size: 13px;
      font-family: "Plus Jakarta Sans", sans-serif;
      outline: none;
      transition: border-color .2s
    }

    .nl-input::placeholder {
      color: rgba(255, 255, 255, .25)
    }

    .nl-input:focus {
      border-color: var(--teal)
    }

    .nl-btn {
      background: var(--teal);
      color: #fff;
      border: none;
      padding: 10px 14px;
      border-radius: var(--r);
      cursor: pointer;
      font-weight: 700;
      font-size: 13px;
      white-space: nowrap;
      transition: background .2s
    }

    .nl-btn:hover {
      background: var(--teal2)
    }

    .socials {
      display: flex;
      gap: 8px;
      margin-top: 18px
    }

    .soc {
      width: 35px;
      height: 35px;
      border: 1.5px solid rgba(255, 255, 255, .1);
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all .2s;
      cursor: pointer
    }

    .soc:hover {
      background: var(--teal);
      border-color: var(--teal);
      transform: translateY(-2px)
    }

    .soc svg {
      width: 14px;
      height: 14px;
      fill: rgba(255, 255, 255, .45)
    }

    .soc:hover svg {
      fill: #fff
    }

    .fcol h4 {
      font-size: 12px;
      font-weight: 800;
      color: #fff;
      margin-bottom: 18px;
      text-transform: uppercase;
      letter-spacing: 1px
    }

    .fcol li {
      margin-bottom: 10px
    }

    .fcol li a {
      font-size: 13px;
      color: rgba(255, 255, 255, .4);
      transition: color .2s
    }

    .fcol li a:hover {
      color: var(--teal)
    }

    .footer-bottom {
      border-top: 1px solid rgba(255, 255, 255, .07);
      padding: 22px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 12px
    }

    .footer-bottom p {
      font-size: 12px;
      color: rgba(255, 255, 255, .24)
    }

    .ftm-links {
      display: flex;
      gap: 20px
    }

    .ftm-links a {
      font-size: 12px;
      color: rgba(255, 255, 255, .24);
      transition: color .2s
    }

    .ftm-links a:hover {
      color: var(--teal)
    }

    .ftm-badges {
      display: flex;
      gap: 8px
    }

    .fbd {
      background: rgba(255, 255, 255, .05);
      border: 1px solid rgba(255, 255, 255, .07);
      border-radius: 5px;
      padding: 4px 10px;
      font-size: 10px;
      font-weight: 700;
      color: rgba(255, 255, 255, .3);
      text-transform: uppercase;
      letter-spacing: .4px
    }

    /* ══ FLOATERS ══ */


    50% {
      box-shadow: 0 8px 32px rgba(37, 211, 102, .7)
    }
    }

    /* ══ ANIMATIONS ══ */
    .fade {
      opacity: 0;
      transform: translateY(26px);
      transition: opacity .7s cubic-bezier(.25, .46, .45, .94), transform .7s
    }

    .fade.in {
      opacity: 1;
      transform: none
    }

    .fade-l {
      opacity: 0;
      transform: translateX(-26px);
      transition: opacity .7s, transform .7s
    }

    .fade-l.in {
      opacity: 1;
      transform: none
    }

    .fade-r {
      opacity: 0;
      transform: translateX(26px);
      transition: opacity .7s, transform .7s
    }

    .fade-r.in {
      opacity: 1;
      transform: none
    }

    /* ══════════════════════════════════════
   RESPONSIVE BREAKPOINTS
══════════════════════════════════════ */

    /* 1100px */
    @media(max-width:1100px) {
      .hero-main-img {
        max-width: 350px
      }

      .stats-row {
        grid-template-columns: repeat(2, 1fr)
      }

      .process-grid {
        grid-template-columns: repeat(2, 1fr)
      }

      .test-grid {
        grid-template-columns: repeat(2, 1fr)
      }

      .why-grid {
        grid-template-columns: 1fr;
        gap: 48px
      }

      .footer-grid {
        grid-template-columns: 1fr 1fr;
        gap: 36px
      }

      .cta-inner {
        padding: 56px 40px;
        flex-direction: column;
        text-align: center
      }

      .cta-right-btns {
        align-items: center;
        width: 100%
      }

      .cta-right-btns .btn {
        width: 100%;
        max-width: 360px;
        min-width: unset
      }
    }

    /* 960px – services 2-col */
    @media(max-width:960px) {
      .srv-grid {
        grid-template-columns: repeat(2, 1fr)
      }
    }

    /* 900px – mobile layout */
    @media(max-width:900px) {

      /* ── Topbar: 2 static rows ── */
      .topbar {
        padding: 7px 14px;
        flex-direction: column;
        align-items: center;
        gap: 5px;
        overflow: visible;
      }

      .topbar-addr {
        justify-content: center;
        text-align: center;
        font-size: 10.5px;
        white-space: normal;
        text-align: center;
        line-height: 1.4;
      }

      .topbar-row2 {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        gap: 14px;
        flex-wrap: wrap;
      }

      .topbar-row2 .tdiv {
        display: flex
      }

      .topbar-emergency {
        display: none
      }

      .topbar>.tdiv {
        display: none
      }

      .ti {
        font-size: 11px;
        gap: 5px
      }

      /* ── Nav ── */
      nav {
        padding: 0 16px;
        height: 70px
      }

      .nav-links {
        display: none
      }

      .nav-right .btn {
        display: none
      }

      .hamburger {
        display: flex
      }

      .logo-wrap img {
        height: 48px
      }

      .logo-company {
        font-size: 16px
      }

      .logo-sub {
        font-size: 14px
      }

      /* ── Hero ── */
      .hero-inner {
        grid-template-columns: 1fr;
        min-height: auto;
        padding: 20px 20px 30px;
        gap: 28px
      }

      .hero-right {
        display: flex;
        padding: 0
      }

      .hero-img-wrap {
        max-width: 100%;
        margin: 0 auto;
        max-width: 280px
      }

      .hero-main-img {
        height: auto;
        max-height: 400px;
        width: 100%;
        object-fit: cover
      }

      .hero-fc1,
      .hero-fc2,
      .hero-img-badge {
        display: none
      }

      .hero-left {
        padding: 0;
        text-align: center;
        align-items: center
      }

      .hero-pill {
        margin: 0 auto 26px
      }

      .hero h1 {
        text-align: center
      }

      .hero-ar-title {
        text-align: center
      }

      .hero-desc {
        text-align: center;
        margin: 0 auto 34px
      }

      .hero-btns {
        justify-content: center;
        flex-direction: column;
        width: 100%
      }

      .hero-btns .btn {
        width: 100%
      }

      .hero-trust {
        justify-content: center;
        flex-wrap: wrap
      }

      .hero-badges {
        justify-content: center
      }

      /* ── Sections ── */
      .section {
        padding: 60px 20px
      }

      .process-bg {
        padding: 60px 20px
      }

      .srv-head {
        flex-direction: column;
        align-items: flex-start
      }

      .srv-head .btn-green {
        width: 100%;
        justify-content: center
      }

      .stats-row {
        grid-template-columns: repeat(2, 1fr)
      }

      .stat-col {
        padding: 32px 16px
      }

      .stat-num {
        font-size: 40px
      }

      /* ── Why video ── */
      .why-video-container {
        max-width: 300px
      }

      /* ── Gallery ── */
      .gal-mosaic {
        grid-template-columns: 1fr 1fr;
        grid-template-rows: repeat(3, 200px);
        gap: 8px
      }

      .gal-item {
        grid-column: auto !important;
        grid-row: auto !important
      }

      .gal-item:nth-child(1) {
        grid-column: span 2 !important;
        height: 200px
      }

      /* ── Reviews ── */
      .test-grid {
        animation: scrollReviews 30s linear infinite
      }

      .test-card {
        width: 320px;
        min-height: 240px
      }

      /* ── CTA ── */
      .cta-outer {
        padding: 40px 0
      }

      .cta-inner {
        padding: 40px 24px
      }

      /* ── Contact bar ── */
      .cbar-inner {
        grid-template-columns: 1fr;
        padding: 0 20px
      }

      .ci {
        border-right: none;
        border-bottom: 1px solid rgba(255, 255, 255, .15);
        padding: 18px 14px
      }

      .ci:last-child {
        border-bottom: none
      }

      /* ── Footer ── */
      .footer-grid {
        grid-template-columns: 1fr 1fr;
        gap: 28px;
        padding: 0 20px 48px
      }

      .footer-grid>.footer-about {
        grid-column: 1 / -1
      }

      .footer-about p {
        max-width: 100%
      }

      .footer-contact-info {
        flex-direction: row;
        flex-wrap: wrap;
        gap: 12px
      }

      .footer-bottom {
        flex-direction: column;
        text-align: center;
        gap: 10px;
        padding: 22px 20px
      }

      .ftm-links,
      .ftm-badges {
        justify-content: center
      }

      /* ── Form ── */
      .form-card {
        padding: 28px 18px
      }

      .form-grid {
        grid-template-columns: 1fr
      }

      .ff.full {
        grid-column: span 1
      }

      .fsr {
        grid-column: span 1
      }

      /* ── Floaters ── */
      .float-wa {
        bottom: 20px;
        left: 20px;
        padding: 16px;
        font-size: 0;
        gap: 0;
        width: 56px;
        height: 56px;
        border-radius: 50%;
        justify-content: center;
        align-items: center;
        box-shadow: 0 8px 28px rgba(37, 211, 102, .6)
      }

      .float-wa:hover {
        box-shadow: 0 12px 40px rgba(37, 211, 102, .9)
      }

      .float-wa svg {
        width: 28px;
        height: 28px
      }
    }

    /* 600px */
    @media(max-width:600px) {
      .topbar {
        padding: 7px 10px;
        gap: 4px
      }

      .topbar-addr {
        font-size: 10px
      }

      .topbar-row2 {
        gap: 10px
      }

      .topbar-row2 .ti {
        font-size: 10.5px
      }

      .ti {
        font-size: 10.5px
      }

      nav {
        height: 60px;
        padding: 0 12px
      }

      .logo-wrap img {
        height: 44px
      }

      .logo-company {
        font-size: 14px
      }

      .logo-sub {
        font-size: 13px
      }

      /* Services 1-col */
      .srv-grid {
        grid-template-columns: 1fr
      }

      .process-grid {
        grid-template-columns: 1fr 1fr;
        gap: 12px
      }

      .step {
        padding: 26px 16px
      }

      /* Reviews */
      .test-grid {
        animation: scrollReviews 25s linear infinite
      }

      .test-card {
        width: 280px;
        min-height: 220px;
        padding: 18px 16px
      }

      /* Hero */
      .hero-inner {
        padding: 15px 16px
      }

      .hero-img-wrap {
        max-width: 240px
      }

      .hero-main-img {
        max-height: 340px
      }

      .hero h1 {
        font-size: 26px
      }

      .hero-ar-title {
        font-size: 18px
      }

      .hero-desc {
        font-size: 14px
      }

      /* Stats */
      .stats-row {
        grid-template-columns: 1fr 1fr
      }

      .stat-num {
        font-size: 30px
      }

      .stat-col {
        padding: 24px 12px
      }

      /* Gallery */
      .gal-mosaic {
        grid-template-columns: 1fr 1fr;
        grid-template-rows: auto;
        gap: 8px
      }

      .gal-item:nth-child(1) {
        grid-column: span 2 !important;
        height: 180px
      }

      .gal-item {
        height: 180px
      }

      /* CTA */
      .cta-inner {
        padding: 32px 18px
      }

      .cta-right-btns .btn {
        width: 100%;
        min-width: unset
      }

      /* Footer */
      .footer-grid {
        grid-template-columns: 1fr;
        gap: 28px
      }

      .footer-contact-info {
        flex-direction: column
      }

      /* Form */
      .nl-row {
        flex-direction: column
      }

      .nl-btn {
        width: 100%
      }

      .why-float {
        display: none
      }
    }

    /* ── FAQ Section ── */
    .faq-wrap {
      max-width: 820px;
      margin: 0 auto
    }

    .faq-item {
      border: 1.5px solid var(--border);
      border-radius: 14px;
      margin-bottom: 12px;
      overflow: hidden;
      transition: box-shadow .25s
    }

    .faq-item:hover {
      box-shadow: 0 4px 18px rgba(0, 0, 0, .07)
    }

    .faq-q {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 20px 24px;
      cursor: pointer;
      gap: 16px;
      user-select: none
    }

    .faq-q-text {
      font-size: 15px;
      font-weight: 700;
      color: var(--navy);
      line-height: 1.45;
      flex: 1
    }

    .faq-icon {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background: var(--off);
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      transition: transform .3s, background .3s
    }

    .faq-item.open .faq-icon {
      transform: rotate(45deg);
      background: var(--teal)
    }

    .faq-item.open .faq-icon svg path {
      fill: #fff
    }

    .faq-a {
      display: none;
      padding: 0 24px 22px;
      font-size: 14px;
      color: var(--gray);
      line-height: 1.85
    }

    .faq-item.open .faq-a {
      display: block
    }

    .faq-a-ar {
      direction: rtl;
      font-family: Tajawal, sans-serif;
      font-size: 15px;
      line-height: 2
    }

    .float-wa {
      position: fixed;
      bottom: 20px;
      right: 20px;
      z-index: 900;
      background: #25D366;
      color: #fff;
      border-radius: 8px;
      padding: 14px 24px;
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 15px;
      font-weight: 700;
      box-shadow: 0 6px 24px rgba(37, 211, 102, .5);
      text-decoration: none;
      transition: all .3s;
      opacity: 0;
      transform: scale(0.8);
      border: none;
      animation: floatPulse 2s ease-in-out infinite
    }

    .float-wa.show {
      opacity: 1;
      transform: scale(1)
    }

    .float-wa:hover {
      box-shadow: 0 12px 40px rgba(37, 211, 102, .8);
      transform: scale(1.05) translateY(-3px)
    }

    .float-wa svg {
      width: 20px;
      height: 20px;
      fill: #fff;
      flex-shrink: 0
    }

    @keyframes floatPulse {

      0%,
      100% {
        box-shadow: 0 6px 24px rgba(37, 211, 102, .5)
      }

      50% {
        box-shadow: 0 8px 32px rgba(37, 211, 102, .7)
      }
    }

    @media(max-width:900px) {
      .float-wa {
        bottom: 20px;
        left: 20px;
        right: auto;
        padding: 16px;
        font-size: 0;
        gap: 0;
        width: 56px;
        height: 56px;
        border-radius: 50%;
        justify-content: center;
        align-items: center;
        box-shadow: 0 8px 28px rgba(37, 211, 102, .6)
      }

      .float-wa svg {
        width: 28px;
        height: 28px
      }
    }
  </style>